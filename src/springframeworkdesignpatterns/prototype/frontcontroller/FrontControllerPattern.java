package springframeworkdesignpatterns.prototype.frontcontroller;

public class FrontControllerPattern {

	public static void main(String[] args) {

		// When we send an HTTP request, 
		// for example, localhost:8080/c2
		// it doesn't go directly to other controller.
		// Instead, it goes to the dispatcher servelet
		// and then this dispatcher servelet
		// sends the request to the corresponding controller
	
	}

}
