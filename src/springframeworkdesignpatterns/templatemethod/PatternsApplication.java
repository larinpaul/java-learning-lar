package springframeworkdesignpatterns.templatemethod;

import org.springframework.jdbc.core.JdbcTemplate;

public class PatternsApplication {

	public static void main(String[] args) {
		
		JdbcTemplate; // This class a lot of boilerplate code that is needed to create a JDBC connection
		// But it's a little bit different that a definition of a template method
		// but, in the end, it's the same concept
		// that we put the boilerplate in our template class

		// There is a slight difference
		// between this design pattern in books
		// and in the Spring implementation of them.
		// But, overall, it's basically the same
		
		
	}

}
